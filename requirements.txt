# Dependencies required to validate committed tables and regenerate current plots.
# PyTorch is optional and is only inspected by scripts/export_env.py when present.

numpy>=1.24,<3
pandas>=2.0,<3
matplotlib>=3.8,<4

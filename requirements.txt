# AniSOAP Optimization — Core + Dev dependencies
# Python 3.8–3.13 compatible • macOS (MPS) and Linux CPU

numpy>=1.24,<3
pandas>=2.0,<3
matplotlib>=3.8,<4
pyyaml>=6.0
torch>=2.6,<2.10  # CPU/MPS build; CUDA not required

# Profiling / visualization (Graphviz system binary recommended for dot rendering)
gprof2dot
py-spy
graphviz

# Testing
pytest
pytest-cov

# Optional pretty console output
# rich>=13

# AniSOAP Optimization — Core runtime dependencies
# Python 3.8–3.11 compatible
# Works on macOS (MPS) and Linux CPU environments

numpy>=1.24,<3
pandas>=2.0,<3
matplotlib>=3.8,<4
pyyaml>=6.0
torch>=2.3,<2.6  # CPU/MPS build; CUDA not required

# AniSOAP Optimization — Development & profiling dependencies

gprof2dot
py-spy
graphviz
pytest
pytest-cov
# rich>=13  # optional pretty console output if you decide to use it
